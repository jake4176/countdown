// 사이트 설정 — 배포 전 이 값들을 실제 값으로 바꾸세요.
// (정적 사이트이므로 Next.js의 NEXT_PUBLIC_* 환경변수 대신 이 파일을 사용합니다.)
window.SITE_CONFIG = {
  // 실제 배포 도메인. 배포 전 반드시 변경. 개발 중에는 비워 두면 현재 origin을 사용합니다.
  siteUrl: '',

  // Google Analytics 측정 ID (예: 'G-XXXXXXXXXX'). 값이 있을 때만 GA가 로드됩니다.
  // 분석 도구를 실제로 켜면 개인정보처리방침에 쿠키/분석 사용을 반드시 반영하세요.
  gaId: '',
};
